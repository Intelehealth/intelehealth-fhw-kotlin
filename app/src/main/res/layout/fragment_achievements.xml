<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View"/>

        <variable
            name="level"
            type="Integer" />

        <variable
            name="points"
            type="Integer" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/std_16dp">

        <ImageView
            android:id="@+id/ivAchievementBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/action_my_achievements"
            android:src="@drawable/ic_achievements_badge"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvUserLevel"
            style="@style/Theme.IDA.TextAppearance.Heading1.Bold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/std_16dp"
            android:text="@{@string/lbl_level(level)}"
            android:textStyle="bold"
            app:layout_constraintBottom_toTopOf="@id/tvUserPoints"
            app:layout_constraintStart_toEndOf="@id/ivAchievementBadge"
            app:layout_constraintTop_toTopOf="@id/ivAchievementBadge"
            tools:text="Level 1" />

        <TextView
            android:id="@+id/tvUserPoints"
            style="@style/Theme.IDA.TextAppearance.Secondary.Medium"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/std_16dp"
            android:lineSpacingExtra="@dimen/std_line_space"
            android:visibility="@{ points > 0 ? View.VISIBLE : View.GONE }"
            android:text="@{String.valueOf(points)}"
            app:layout_constraintBottom_toBottomOf="@id/ivAchievementBadge"
            app:layout_constraintStart_toEndOf="@id/ivAchievementBadge"
            app:layout_constraintTop_toBottomOf="@id/tvUserLevel"
            tools:text="1200 Points" />

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabAchievements"
            style="@style/Theme.IDA.TabLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/std_16dp"
            android:gravity="center"
            android:textAlignment="center"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/ivAchievementBadge" />

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/pagerAchievements"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:visibility="visible"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tabAchievements" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
